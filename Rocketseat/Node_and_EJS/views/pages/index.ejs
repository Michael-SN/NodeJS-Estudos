<%- include('../partials/head'); -%>
  <%- include('../partials/header') %>
    <main>
      <div class="p-5 mb-4 bg-light rounded-3">
        <h1>Node with EJS</h1>
        <p>Let's learn how to build a webpage with nodeJs and EJS</p>
      </div>

      <ul>
        <% qualitys.forEach(function(quality) { %>
          <li>
            <strong>
              <%= quality.title %>
            </strong>
            <%= quality.message %>
          </li>
          <% }) %>
      </ul>

    </main>
    <%- include('../partials/footer') %>